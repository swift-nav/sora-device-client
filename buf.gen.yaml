version: v1
managed:
  enabled: true
plugins:
  - remote: buf.build/protocolbuffers/plugins/python:v21.1.0-1
    # python version in output path needs to be kept in sync with `requires-python`
    # and `pythonVersion` keys in pyproject.toml
    out: __pypackages__/3.10/lib

    # remote plugin version needs to be kept in sync with version of grpc-io in pyproject.toml
  - remote: buf.build/grpc/plugins/python:v1.46.3-1
    # python version in output path needs to be kept in sync with `requires-python`
    # and `pythonVersion` keys in pyproject.toml
    out: __pypackages__/3.10/lib
